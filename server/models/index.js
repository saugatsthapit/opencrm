/**
 * Models for the application
 * These are placeholder model definitions that match what's being imported in routes/calls.js
 */

// Call model - represents a call made through the API
const Call = {
  // Methods and properties would go here
  // This is just a stub to prevent the import error
};

// CallHistory model - represents the history of calls for a lead
const CallHistory = {
  // Methods and properties would go here
  // This is just a stub to prevent the import error
};

module.exports = {
  Call,
  CallHistory
}; 